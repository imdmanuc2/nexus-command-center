# Package 017 — Operations Automation Engine

Adds a guarded action catalog and persistent automation runs. High-risk actions require approval; dry-run is the default; arbitrary shell commands are not accepted.

```bash
chmod +x scripts/*.sh scripts/*.py
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
