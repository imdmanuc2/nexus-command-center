# Changelog

- Match Seymour `cpu01` to the existing CPU Mining VM asset.
- Derive worker type from CMDB classification instead of defaulting to ASIC.
- Preserve explicit worker-to-asset mappings in Seymour connector configuration.
- Render CPU miner type and live MH/s on the Canvas.
