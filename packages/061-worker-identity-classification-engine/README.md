# Package 061 — Worker Identity & Classification Engine

Resolves live mining sessions to physical CMDB assets using explicit mappings, worker suffixes, and remote-host evidence. Correctly classifies CPU, GPU, FPGA, and ASIC workers and updates the Infrastructure Canvas to display the matched miner type and live hashrate.
