# Verification

Run:

```bash
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```

The verifier creates a low-risk test change, validates approval-free lifecycle behavior, checks all APIs, and removes the test data afterward.
