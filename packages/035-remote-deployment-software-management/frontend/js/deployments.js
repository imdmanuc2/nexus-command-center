const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const root=document.querySelector('#jobs');try{const r=await fetch('/api/platform/deployments/jobs');const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');const jobs=d.jobs||[];document.querySelector('#jobCount').textContent=jobs.length;document.querySelector('#runningCount').textContent=jobs.filter(j=>j.status==='running').length;document.querySelector('#failedCount').textContent=jobs.filter(j=>['failed','partial'].includes(j.status)).length;root.innerHTML=jobs.length?jobs.map(j=>`<article class="job"><div><strong>${esc(j.name)}</strong><div class="muted">${esc(j.jobId)}</div></div><div class="status">${esc(j.status)}</div><div>${esc(j.strategy)}</div><div class="muted">${esc(new Date(j.requestedAt).toLocaleString())}</div></article>`).join(''):'<p class="muted">No deployment jobs yet.</p>'}catch(e){root.textContent=e.message}}document.querySelector('#refresh').addEventListener('click',load);load();