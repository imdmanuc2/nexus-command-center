# Package 050 — Operations Center & Evidence UI Integration

Adds a dedicated Operations Center using Package 049 evidence APIs, registers it in Nexus navigation, and adds a Home V2 operational evidence summary.

```bash
chmod +x scripts/*.sh
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
