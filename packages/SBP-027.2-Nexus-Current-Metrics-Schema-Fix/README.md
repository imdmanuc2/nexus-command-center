# SBP-027.2 — Nexus Current Metrics Schema Fix

Aligns runtime-state metric writes with the real Nexus current_metrics schema, keeps runtimeState canonical in observed_state JSONB, and updates SBP-028 acceptance.
