async function load(){const catalog=await fetch('/api/playbooks').then(r=>r.json());document.querySelector('#catalog').innerHTML=(catalog.playbooks||[]).map(p=>`<article><h2>${p.name}</h2><p>${p.description}</p><small>${p.playbookId} · ${p.riskLevel} · ${p.stepCount} steps</small></article>`).join('');const runs=await fetch('/api/playbooks/runs?limit=20').then(r=>r.json());document.querySelector('#runs').textContent=JSON.stringify(runs.runs||[],null,2)}load().catch(e=>document.body.insertAdjacentHTML('beforeend',`<pre>${e}</pre>`));
