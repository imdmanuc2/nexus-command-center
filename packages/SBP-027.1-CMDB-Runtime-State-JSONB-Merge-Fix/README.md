# SBP-027.1 — CMDB Runtime State JSONB Merge Fix

Preserves rich JSONB observed_state, merges normalized runtime state into it, and updates SBP-028 live acceptance to validate observed_state.runtimeState.
