# Verification

The verification script confirms the schema, Python imports, API routes, maintenance-window effective-state integration, alert suppression policy, audit path, bulk operations, and frontend assets. A live API request is attempted when port 8080 is reachable.
