# Package 021 — Platform Stabilization

Fixes the alert INSERT alignment and hardens JSONB serialization before the Operations Center UI work begins.

```bash
chmod +x scripts/*.sh scripts/*.py
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
