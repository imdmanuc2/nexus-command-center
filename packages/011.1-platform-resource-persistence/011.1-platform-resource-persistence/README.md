# Package 011.1 — Platform Resource Persistence

Persists live blockchain nodes and MiningCore connector instances into PostgreSQL and integrates both into the existing one-minute Platform sync.

```bash
chmod +x scripts/*.sh scripts/*.py
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```

Expected counts: 1 blockchain node and 2 MiningCore instances.
