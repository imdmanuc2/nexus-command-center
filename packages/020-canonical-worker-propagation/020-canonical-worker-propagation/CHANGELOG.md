# Changelog

## 1.0.0
- Captures the worker returned by `upsert_worker()`.
- Uses the persisted canonical worker ID for workloads.
- Uses the same canonical worker ID for worker relationships.
- Adds integrated sync and foreign-key verification.
