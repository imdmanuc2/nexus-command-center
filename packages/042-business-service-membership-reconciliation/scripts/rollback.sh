#!/usr/bin/env bash
set -euo pipefail
echo "Use the timestamped backup printed by install.sh to restore application files."
echo "Database migration 031 is additive and is intentionally not dropped automatically."
