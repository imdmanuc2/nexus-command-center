# Package 041 — Operations Center & Service Health

Adds an operator-focused business-service dashboard, dedicated platform health endpoint, service-health and capacity APIs, live incident correlation, redundancy reporting, and database foundations for health snapshots, incidents, and availability rollups.

## Install

```bash
chmod +x scripts/*.sh
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
