# Package 040 — Service Topology & Business Services

Adds CMDB-authoritative business services with member assets, service-to-service dependencies, health and capacity rollups, workload-aware templates for mining, AI compute, and rentable compute, API endpoints, and a Service Topology page.

## Install

```bash
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```
