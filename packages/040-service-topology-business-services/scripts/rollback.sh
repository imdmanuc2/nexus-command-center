#!/usr/bin/env bash
set -euo pipefail
echo "Use the timestamped backup created by install.sh to restore backend/api/server.py and frontend/js/nav.js. Database tables are intentionally retained to preserve CMDB history."
