# Verify Package 044

Run:

```bash
./scripts/doctor.sh
./scripts/install.sh
./scripts/verify.sh
```

Verification checks migration objects, Python compilation, API routes, Operations Center integration, and live JSON responses from every Package 044 endpoint.
